const Loading = () => (
  <div className="fixed inset-0 z-[100] min-h-screen bg-[#0a0d14] flex items-center justify-center text-white">
    <div className="animate-spin rounded-full h-10 w-10 border-t-2 border-b-2 border-[#f59e0b]"></div>
  </div>
);

export default Loading;
